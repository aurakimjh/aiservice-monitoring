# 단위테스트 1차 테스트 절차서

> **프로젝트**: AITOP AI Service Monitoring Platform
> **테스트 유형**: 단위테스트
> **차수**: 1차
> **계획일**: 2026-03-24
> **작성자**: Aura Kim

---

## 1. 테스트 목표

Phase 1~30 전체 구현 완료 후, Backend(Go Agent) + Frontend(Next.js) 단위테스트를 AI와 수동으로 교차 검증하여 코드 품질과 안정성을 확인한다.

## 2. 테스트 범위

### 2-1. 포함 (In-Scope)

| # | 대상 영역 | 테스트 항목 | 기대 결과 |
|---|----------|-----------|-----------|
| 1 | Go Agent 빌드 | `go build ./...` | 컴파일 에러 0 |
| 2 | Go 단위 테스트 | 21개 테스트 파일, ~150개 테스트 | ALL PASS |
| 3 | Go 커버리지 | 패키지별 커버리지 측정 | 수치 기록 |
| 4 | Frontend 빌드 | `npx next build` | 컴파일 에러 0 |
| 5 | Frontend Vitest | 5개 테스트 파일, ~15개 테스트 | ALL PASS |
| 6 | Frontend 커버리지 | V8 커버리지 측정 | 60% 이상 (설정 기준) |
| 7 | AI-L1 코드 품질 | any 타입, 에러 무시, 시크릿 하드코딩 | 스캔 결과 기록 |
| 8 | AI-L2 커버리지 갭 | 테스트 미비 패키지/컴포넌트 | 갭 목록 기록 |

### 2-2. 제외 (Out-of-Scope)

| # | 제외 항목 | 제외 사유 |
|---|----------|----------|
| 1 | Docker 통합 테스트 | 통합테스트 차수에서 수행 |
| 2 | Playwright E2E | E2E테스트 차수에서 수행 |
| 3 | 부하 테스트 | E2E테스트 차수에서 수행 |
| 4 | 보안 감사 | E2E테스트 차수에서 수행 |

## 3. 사전 조건

| # | 조건 | 확인 명령어 | 기대 결과 |
|---|------|-----------|-----------|
| 1 | Go 1.25+ 설치 | `go version` | `go version go1.25.x` |
| 2 | Node.js 22+ 설치 | `node --version` | `v22.x.x` |
| 3 | npm 설치 | `npm --version` | 버전 출력 |
| 4 | 워킹 디렉토리 클린 | `git status` | `working tree clean` |

---

## 4. AI 실행 절차 (Claude Code)

> AI가 먼저 실행하고, 결과를 `결과서_단위테스트_1차_AI.md`에 기록합니다.

### Step A-1: 환경 검증

```bash
go version && node --version && npm --version
```

- **기대 결과**: Go 1.25+, Node 22+, npm 버전 출력
- **판정 기준**: 모든 버전이 최소 요구사항 충족 시 PASS

### Step A-2: Go 빌드 검증

```bash
cd agent && go build ./...
```

- **기대 결과**: 출력 없음 (성공)
- **판정 기준**: exit code 0이면 PASS

### Step A-3: Go 단위 테스트 실행

```bash
cd agent && go test -v -race -count=1 ./... 2>&1 | tee ../test/단위테스트_1차_2026-03-24/logs/go-test-output.txt
```

- **기대 결과**: 모든 패키지 `ok` 또는 `[no test files]`
- **판정 기준**: FAIL 0건이면 PASS

**대상 패키지 (21개 테스트 파일)**:

| # | 패키지 | 분류 |
|---|--------|------|
| 1 | `cmd/collection-server` | 서버 |
| 2 | `internal/collector/os` | IT Collector |
| 3 | `internal/collector/web` | IT Collector |
| 4 | `internal/collector/was` | IT Collector |
| 5 | `internal/collector/db` | IT Collector |
| 6 | `internal/collector/cache` | IT Collector |
| 7 | `internal/collector/mq` | IT Collector |
| 8 | `internal/collector/ai/llm` | AI Collector |
| 9 | `internal/collector/ai/otel` | AI Collector |
| 10 | `internal/collector/ai/vectordb` | AI Collector |
| 11 | `internal/health` | Core |
| 12 | `internal/output` | Core |
| 13 | `internal/shell` | Core |
| 14 | `internal/statemachine` | Core |
| 15 | `internal/transport` (heartbeat) | Transport |
| 16 | `internal/transport` (prometheus) | Transport |
| 17 | `internal/updater` | Core |
| 18 | `pkg/storage` (keys) | Storage |
| 19 | `pkg/storage` (local_backend) | Storage |
| 20 | `pkg/storage` (dual_backend) | Storage |
| 21 | `pkg/storage` (factory, s3_backend) | Storage |

### Step A-4: Go 커버리지 리포트

```bash
cd agent && go test -coverprofile=coverage.out ./... && go tool cover -func=coverage.out | tail -1
```

- **기대 결과**: 전체 커버리지 수치 출력
- **판정 기준**: 수치 기록 (임계값 없이 현황 파악)

### Step A-5: Frontend 빌드 검증

```bash
cd frontend && npx next build
```

- **기대 결과**: `✓ Compiled successfully`
- **판정 기준**: 빌드 성공 시 PASS

### Step A-6: Frontend Vitest 실행

```bash
cd frontend && npx vitest run --reporter=verbose 2>&1 | tee ../test/단위테스트_1차_2026-03-24/logs/vitest-output.txt
```

- **기대 결과**: 모든 테스트 통과
- **판정 기준**: FAIL 0건이면 PASS

**대상 파일 (5개)**:

| # | 테스트 파일 | 대상 |
|---|-----------|------|
| 1 | `components/ui/__tests__/button.test.tsx` | Button 컴포넌트 |
| 2 | `hooks/__tests__/use-i18n.test.ts` | i18n 훅 |
| 3 | `lib/__tests__/i18n.test.ts` | i18n 라이브러리 |
| 4 | `lib/__tests__/utils.test.ts` | 유틸리티 함수 |
| 5 | `stores/__tests__/ui-store.test.ts` | UI 상태 스토어 |

### Step A-7: Frontend 커버리지 리포트

```bash
cd frontend && npx vitest run --coverage 2>&1 | tee ../test/단위테스트_1차_2026-03-24/logs/coverage-summary.txt
```

- **기대 결과**: 커버리지 수치 출력
- **판정 기준**: 60% 이상이면 PASS (vitest.config.ts 설정 기준)

### Step A-8: AI-L1 코드 품질 분석

Claude Code가 코드베이스를 분석하여 다음을 스캔:

| # | 점검 항목 | 대상 | 방법 |
|---|----------|------|------|
| 1 | `any` 타입 사용 | `frontend/src/**/*.{ts,tsx}` | grep 스캔 |
| 2 | 에러 무시 패턴 | `agent/**/*.go` | `_ = err` 패턴 스캔 |
| 3 | 하드코딩 시크릿 | 전체 | password, secret, api_key 리터럴 스캔 |
| 4 | 미사용 import | `frontend/src/**/*.{ts,tsx}` | lint 기반 |

- **판정 기준**: 각 항목 건수 기록 (정보 제공, 블로커 아님)

### Step A-9: AI-L2 커버리지 갭 분석

| # | 점검 항목 | 방법 |
|---|----------|------|
| 1 | Go `[no test files]` 패키지 | Step A-3 출력에서 추출 |
| 2 | Frontend 테스트 미비 컴포넌트 | 48 페이지 vs 5 테스트 파일 비교 |
| 3 | 우선순위 분류 | Critical/High/Medium/Low |

- **판정 기준**: 갭 목록 기록 (정보 제공)

### Step A-10: 결과서 작성

- `결과서_단위테스트_1차_AI.md` 작성
- 모든 수치/로그 참조 포함

---

## 5. 수동 검증 절차 (사용자)

> AI 결과를 참조하면서 수동으로 재검증합니다.
> 결과를 `결과서_단위테스트_1차_수동.md`에 기록합니다.

### Step M-1: Go 테스트 직접 실행

- **AI 결과 참조**: Step A-3
- **수동 확인**: `cd agent && go test ./... -v` 직접 실행
- **판정 기준**: AI 결과와 PASS/FAIL 건수 일치 여부

### Step M-2: FAIL 항목 재확인

- **AI 결과 참조**: Step A-3 FAIL 목록
- **수동 확인**: FAIL 발생 시 해당 테스트만 단독 실행하여 재현 확인
- **판정 기준**: 재현 가능 여부 + 원인 분석

### Step M-3: Frontend Vitest 직접 실행

- **AI 결과 참조**: Step A-6
- **수동 확인**: `cd frontend && npx vitest run` 직접 실행
- **판정 기준**: AI 결과와 PASS/FAIL 건수 일치 여부

### Step M-4: 커버리지 리포트 열람

- **AI 결과 참조**: Step A-4, A-7
- **수동 확인**: 브라우저에서 커버리지 HTML 리포트 열기
- **판정 기준**: 수치가 AI 보고와 일치하는지 확인

### Step M-5: AI 코드 품질 결과 검토

- **AI 결과 참조**: Step A-8
- **수동 확인**: AI가 지적한 `any` 타입, 에러 무시 위치를 직접 파일에서 확인
- **판정 기준**: 지적 내용이 실제 코드와 일치하는지 (False Positive 여부)

### Step M-6: 커버리지 갭 우선순위 합의

- **AI 결과 참조**: Step A-9
- **수동 확인**: 갭 목록을 검토하고, 다음 차수에서 보완할 우선순위 결정
- **판정 기준**: 합의된 우선순위 목록 도출

---

## 6. 교차검증 기준

| 항목 | AI 판정 | 수동 판정 | 최종 판정 규칙 |
|------|--------|----------|--------------|
| Go 빌드 | | | 양쪽 PASS → PASS |
| Go 단위 테스트 | | | 양쪽 동일 결과 → 확정, 불일치 → 재실행 |
| Frontend 빌드 | | | 양쪽 PASS → PASS |
| Frontend Vitest | | | 양쪽 동일 결과 → 확정, 불일치 → 재실행 |
| 커버리지 수치 | | | 차이 5% 이내 → PASS |
| 코드 품질 | | | AI 지적 중 수동 확인된 건만 반영 |

## 7. 완료 조건

- [ ] AI 테스트 전체 실행 완료 (Step A-1 ~ A-10)
- [ ] 수동 검증 전체 실행 완료 (Step M-1 ~ M-6)
- [ ] 교차검증 대조표 작성 완료
- [ ] 변경이력 기록 완료 (발생 시)
- [ ] 결과서 2부 (AI + 수동) 작성 완료
