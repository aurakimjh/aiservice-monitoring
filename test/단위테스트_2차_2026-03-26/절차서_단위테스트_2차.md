# 단위테스트 2차 테스트 절차서

> **프로젝트**: AITOP AI Service Monitoring Platform
> **테스트 유형**: 단위테스트
> **차수**: 2차
> **계획일**: 2026-03-26
> **작성자**: Aura Kim
>
> **이전 차수**: [1차 (2026-03-24)](../단위테스트_1차_2026-03-24/절차서_단위테스트_1차.md)
> **실행 환경**: Windows PowerShell 사용 시 [OS별 명령어 참조](../templates/OS별_명령어_참조.md) 참고

---

## 1. 테스트 목표

Phase 31~38 신규 기능 구현 완료 후, 신규 패키지의 단위 테스트를 포함한 전체 30개 테스트 파일 검증.
1차(2026-03-24, Phase 30 기준) 대비 변경 사항을 교차 검증한다.

## 2. 1차 대비 변경 사항

| Phase | 신규 패키지 | 테스트 파일 현황 |
|-------|-----------|----------------|
| 31 | `internal/collector/evidence` | `equivalence_test.go` — 신규 |
| 31 | `internal/script` | `executor_test.go` — 신규 |
| 32 | `internal/collector/ai/gpu` | `gpu_collector_test.go` — 신규 |
| 34 | `internal/attach` | `attach_test.go` — 신규 |
| 16 | `internal/lite` | `pdf_test.go` — 신규 |
| 31-38 | `internal/collector/batch` | 테스트 없음 — 작성 필요 |
| 35 | `internal/collector/perfebpf` | 테스트 없음 — 작성 필요 |
| 33 | `internal/plugin` | 테스트 없음 — 작성 필요 |

**전체 테스트 파일**: 21개 (1차) → **30개** (2차)

## 3. 테스트 범위

### 3-1. 포함 (In-Scope)

| # | 대상 영역 | 테스트 항목 | 기대 결과 |
|---|----------|-----------|-----------|
| 1 | Go Agent 빌드 | `go build ./...` (Phase 38 포함) | 컴파일 에러 0 |
| 2 | Go 단위 테스트 | 30개 테스트 파일 전체 | ALL PASS |
| 3 | 신규 패키지 테스트 | evidence, script, gpu, attach, lite | ALL PASS |
| 4 | 신규 패키지 빌드 | batch, perfebpf, plugin, diagnose | 빌드 오류 0 |
| 5 | Frontend 빌드 | `npx next build` (배치 UI 5페이지 포함) | 컴파일 에러 0 |
| 6 | Frontend Vitest | 5개 테스트 파일 | ALL PASS |
| 7 | AI-L2 커버리지 갭 | Phase 31-38 미테스트 패키지 식별 | 갭 목록 기록 |

### 3-2. 제외 (Out-of-Scope)

| # | 제외 항목 | 제외 사유 |
|---|----------|----------|
| 1 | perf/eBPF 실행 테스트 | Linux 전용 환경 필요 |
| 2 | 실제 프로세스 Attach | 특정 런타임 환경 필요 |
| 3 | 배치 프로세스 감지 | 실행 중 배치 작업 필요 |

## 4. 사전 조건

| # | 조건 | 확인 명령어 | 기대 결과 |
|---|------|-----------|-----------|
| 1 | Go 1.25+ 설치 | `go version` | `go version go1.25.x` |
| 2 | Node.js 22+ 설치 | `node --version` | `v22.x.x` |
| 3 | 워킹 디렉토리 클린 | `git status` | `working tree clean` |

---

## 5. AI 실행 절차 (Claude Code)

### Step A-1: 환경 검증

```bash
go version && node --version && npm --version
```

- **기대 결과**: Go 1.25+, Node 22+

### Step A-2: Go 빌드 검증 (Phase 38 전체)

```bash
cd agent && go build ./...
```

- **기대 결과**: 출력 없음 (성공)

### Step A-3: 신규 패키지 빌드 확인

```bash
cd agent
go build ./internal/attach/...
go build ./internal/collector/batch/...
go build ./internal/collector/perfebpf/...
go build ./internal/collector/profiling/...
go build ./internal/plugin/...
go build ./internal/diagnose/...
```

- **기대 결과**: 각 패키지 컴파일 성공

### Step A-4: Go 단위 테스트 전체 실행 (30개 파일)

```bash
cd agent && go test -v -count=1 ./... 2>&1 | tee ../test/단위테스트_2차_2026-03-26/logs/go-test-output.txt
```

- **기대 결과**: FAIL 0건

**신규 테스트 패키지 목록**:

| # | 패키지 | 파일 | 신규 여부 |
|---|--------|------|---------|
| 1 | `internal/attach` | `attach_test.go` | **신규** (Phase 34) |
| 2 | `internal/collector/evidence` | `equivalence_test.go` | **신규** (Phase 31) |
| 3 | `internal/collector/ai/gpu` | `gpu_collector_test.go` | **신규** (Phase 32) |
| 4 | `internal/script` | `executor_test.go` | **신규** (Phase 31) |
| 5 | `internal/lite` | `pdf_test.go` | **신규** |
| 6 | `internal/collector/middleware` | `middleware_collector_test.go` | **신규** |

### Step A-5: Go 커버리지 리포트

```bash
cd agent && go test -coverprofile=coverage.out ./... && go tool cover -func=coverage.out | tail -1
```

### Step A-6: Frontend 빌드 검증 (배치 UI 5페이지 포함)

```bash
cd frontend && npx next build
```

- **확인 항목**: Route 테이블에 `/batch`, `/batch/[name]`, `/batch/alerts`, `/batch/executions/[id]`, `/batch/xlog` 포함 여부

### Step A-7: Frontend Vitest

```bash
cd frontend && npx vitest run --reporter=verbose 2>&1 | tee ../test/단위테스트_2차_2026-03-26/logs/vitest-output.txt
```

### Step A-8: AI-L2 — Phase 31-38 커버리지 갭 분석

Phase 31-38에서 추가된 패키지 중 테스트 미비 영역:

| # | 패키지 | 우선순위 | 이유 |
|---|--------|---------|------|
| 1 | `internal/collector/batch` | **High** | 배치 감지 핵심 로직 |
| 2 | `internal/collector/batch/profiler` | **High** | 런타임 프로파일링 |
| 3 | `internal/collector/perfebpf` | **High** | eBPF 수집 로직 |
| 4 | `internal/plugin` | Medium | 플러그인 배포 흐름 |
| 5 | `internal/diagnose` | Medium | 진단 모드 로직 |

---

## 6. 수동 검증 절차

### Step M-1: Go 테스트 직접 실행

```bash
cd /c/workspace/aiservice-monitoring/agent
go test ./... -v
```

### Step M-2: 신규 패키지 개별 테스트

```bash
go test ./internal/attach/... -v
go test ./internal/collector/evidence/... -v
go test ./internal/script/... -v
go test ./internal/collector/ai/gpu/... -v
go test ./internal/lite/... -v
```

### Step M-3: Frontend Vitest 직접 실행

```bash
cd /c/workspace/aiservice-monitoring/frontend
npx vitest run
```

### Step M-4: 배치 UI 페이지 브라우저 확인

```bash
cd /c/workspace/aiservice-monitoring/frontend
npm run dev
```

브라우저에서 확인:
- http://localhost:3000/batch
- http://localhost:3000/batch/alerts
- http://localhost:3000/batch/xlog

---

## 7. 교차검증 기준

| 항목 | AI 판정 | 수동 판정 | 최종 판정 규칙 |
|------|--------|----------|--------------|
| Go 빌드 (Phase 38) | | | 양쪽 PASS → PASS |
| 신규 패키지 빌드 | | | 양쪽 PASS → PASS |
| Go 단위 테스트 (30개) | | | 양쪽 동일 결과 → 확정 |
| Frontend 빌드 (배치 포함) | | | 양쪽 PASS → PASS |
| Frontend Vitest | | | FAIL 0건 → PASS |
| 커버리지 갭 (Phase 31-38) | | | 갭 목록 합의 |

## 8. 완료 조건

- [ ] AI 테스트 전체 실행 완료 (Step A-1 ~ A-8)
- [ ] 수동 검증 전체 실행 완료 (Step M-1 ~ M-4)
- [ ] 교차검증 대조표 작성 완료
- [ ] 결과서 2부 (AI + 수동) 작성 완료
